# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TranslateApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TranslateApp
  pod 'IQKeyboardManagerSwift', '~> 6.5.6'  # keyboard management
  pod 'RxCocoa',          '~> 5.1.1'  # Observer pattern, binding data
  pod 'RxSwift',          '~> 5.1.1'  # Observer pattern, binding data
  pod 'ADCountryPicker', '~> 2.1.0'
  
  pod 'Google-Mobile-Ads-SDK'
  target 'TranslateAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TranslateAppUITests' do
    # Pods for testing
  end

end
